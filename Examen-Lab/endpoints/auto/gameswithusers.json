{
    "getAll": {
        "description": "Gets all entries from 'gameswithusers'",
        "route": "/gameswithusers",
        "method": "GET",
        "sql": "SELECT * FROM gameswithusers",
        "auth_required": false,
        "allowed_roles": [
            "*"
        ]
    }
}