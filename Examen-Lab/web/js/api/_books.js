/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const booksAPI_auto = {

    /** Gets all entries from 'books' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/books`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'books' by its primary key */
    getById: async function(bookId) {
        let response = await axios.get(`${BASE_URL}/books/${bookId}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'books' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/books`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'books' by its primary key */
    update: async function(formData, bookId) {
        let response = await axios.put(`${BASE_URL}/books/${bookId}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'books' by its primary key */
    delete: async function(bookId) {
        let response = await axios.delete(`${BASE_URL}/books/${bookId}`, requestOptions);
        return response.data;
    },
};

export { booksAPI_auto };